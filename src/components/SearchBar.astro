<header class="page-header">
  <div class="search-container">
    <input
      data-search-input
      type="text"
      class="search-input"
      placeholder="Not yet..."
      disabled="true"
    />
    <span class="search-icon">🔍</span>
  </div>
</header>

<style>
  .page-header {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 20dvh;
  }

  .search-container {
    position: relative;
    width: 60%;
  }

  .search-input {
    width: 100%;
    padding: 12px 48px 12px 16px; /* padding right suficiente para el ícono */
    font-size: 16px;
    border: 2px solid #ccc;
    border-radius: 20px;
    outline: none;
    transition:
      border-color 0.3s,
      box-shadow 0.3s;
    background-color: #c2c1c1;
    color: #333;
    box-sizing: border-box;
  }

  .search-input:focus {
    border-color: #1e90ff;
    box-shadow: 0 0 0 3px rgba(30, 144, 255, 0.2);
  }

  .search-icon {
    position: absolute;
    right: 16px; /* posicionado respecto al .search-container */
    top: 50%;
    transform: translateY(-50%);
    font-size: 18px;
    color: #888;
    pointer-events: none;
  }
</style>
